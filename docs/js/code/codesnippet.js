function updateCodeSnippet(calculator){

  if(calculator !=null){
    console.log(calculator)
    // document.getElementById("console").innerHTML = AndroidSpringAnimation(calculator.stiffness,calculator.dampingratio,calculator.velocity);
  }
}


function AndroidSpringAnimation(stiffness,dampingRatio,velocity){

    // var mString = 
    // "SpringAnimation springAnimation = new SpringAnimation([view],[property])\
    // is \

    // // `SpringForce springForce = new SpringForce();\n` + 
    // `springForce.setFinalPosition([finalPosition]);\n` + 
    // `springForce.setStiffness(` + stiffness.toString() + `f);\n` + 
    // `springForce.setDampingRatio(` + dampingRatio.toString() + `f);\n` +
    // `springAnimation.setSpring(springForce);\n` +
    // `springAnimation.start();\n` 

    return AndroidSpringAnimation;
}